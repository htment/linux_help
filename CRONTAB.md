Вот несколько примеров расписаний для crontab с пояснениями:

🔹 Основной синтаксис
```
минута час день_месяца месяц день_недели команда
* — любое значение (каждую минуту, каждый час и т. д.).

*/N — каждые N минут/часов/дней.

A,B,C — конкретные значения (например, 1,15,30).

A-B — диапазон (например, 1-5 — с понедельника по пятницу).
```
🔹 Примеры расписаний
1. Каждую минуту
```
* * * * * /path/to/command
```
2. Каждые 5 минут
```
*/5 * * * * /path/to/command
```
3. Каждый час (в начале часа)
```
0 * * * * /path/to/command
```
4. Каждые 2 часа
```
0 */2 * * * /path/to/command
```
5. Каждый день в 3:00
```
0 3 * * * /path/to/command
```
6. Каждый день в 14:30
 ```
30 14 * * * /path/to/command
```
8. По рабочим дням (Пн–Пт) в 9:00
```
0 9 * * 1-5 /path/to/command
```
9. По выходным (Сб–Вс) в 10:00
```
0 10 * * 6,7 /path/to/command
```
10. 1-го числа каждого месяца в 6:00
```
0 6 1 * * /path/to/command
```
11. Каждые 30 минут с 9:00 до 18:00 по будням
```
*/30 9-18 * * 1-5 /path/to/command
```
12. Раз в год (1 января в 00:00)
```
0 0 1 1 * /path/to/command
```
13. Каждые 10 минут, но только в декабре
```
*/10 * * 12 * /path/to/command
```
14. В 15:00 каждый понедельник и пятницу
```
0 15 * * 1,5 /path/to/command
```
15. Раз в 5 часов, но только днём (8:00–20:00)
```
0 8-20/5 * * * /path/to/command
```
16. Каждые 15 минут, но только с понедельника по пятницу
```
*/15 * * * 1-5 /path/to/command
```
🔹 Специальные строки (если поддерживаются)
Некоторые cron-демоны (например, в Ubuntu) поддерживают сокращённые записи:

Строка	Значение
```
@reboot	При перезагрузке системы
@yearly	Раз в год (0 0 1 1 *)
@annually	То же, что @yearly
@monthly	Раз в месяц (0 0 1 * *)
@weekly	Раз в неделю (0 0 * * 0)
@daily	Раз в день (0 0 * * *)
@midnight	То же, что @daily
@hourly	Раз в час (0 * * * *)
```
Пример:

```
@daily /path/to/backup.sh  # Запускать каждый день в 00:00
```
🔹 Как проверить расписание?
Просмотр текущих задач:

```
crontab -l
```
Проверка логов cron (Ubuntu/Debian):

bash
```
grep CRON /var/log/syslog
```
или

```
journalctl -u cron -f
```
  # Для systemd
🔹 Важно!
Всегда используйте полные пути (/usr/bin/python3, а не просто python3).

Перенаправляйте вывод в лог, если нужно отслеживать ошибки:

bash
* * * * * /path/to/command >> /var/log/cron.log 2>&1
